version: 1.3.0
variables:
  COMPOSER_BIN: $SOURCE_DIR/vendor/bin
  BLT_DIR: $SOURCE_DIR/vendor/acquia/blt
events:
  build:
    steps:
      - hello:
          type: script
          script:
            - echo "Success 😀"
